# git-cliff configuration for disrex/sample-data-themes
# https://git-cliff.org/docs/configuration

[changelog]

# Replace the placeholder REPO URL with the canonical GitHub repo on
# every render. Keeps the template portable if we ever migrate.
postprocessors = [
    { pattern = '<REPO>', replace = "https://github.com/disrex-group/sample-data-themes" },
]

header = """
# Sample Data Themes — Changelog\n
Theme-based demo data for Magento 2 / MageOS. All notable changes to
this project are documented here.\n\n
"""

# Body template — Keats Tera syntax. See https://keats.github.io/tera/docs/
body = """
{%- macro remote_url() -%}
  <REPO>
{%- endmacro -%}

{% macro print_commit(commit) -%}
    - {% if commit.scope %}*({{ commit.scope }})* {% endif %}\
        {% if commit.breaking %}[**breaking**] {% endif %}\
        {{ commit.message | upper_first }} - \
        ([{{ commit.id | truncate(length=7, end="") }}]({{ self::remote_url() }}/commit/{{ commit.id }}))\
{% endmacro -%}

{% if version %}\
    {% if previous.version %}\
        ## [{{ version | trim_start_matches(pat="v") }}]\
          ({{ self::remote_url() }}/compare/{{ previous.version }}..{{ version }}) - {{ timestamp | date(format="%Y-%m-%d") }}
    {% else %}\
        ## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
    {% endif %}\
{% else %}\
    ## [unreleased]
{% endif %}\

{% for group, commits in commits | group_by(attribute="group") %}
    ### {{ group | striptags | trim | upper_first }}
    {% for commit in commits
    | filter(attribute="scope")
    | sort(attribute="scope") %}
        {{ self::print_commit(commit=commit) }}
    {%- endfor %}
    {% for commit in commits %}
        {%- if not commit.scope -%}
            {{ self::print_commit(commit=commit) }}
        {% endif -%}
    {% endfor -%}
{% endfor -%}
"""

footer = """
<!-- Generated by git-cliff -->
"""

trim = true
render_always = true

[git]
conventional_commits = true
filter_unconventional = true
split_commits = false

commit_preprocessors = [
    # Auto-link issue references like (#123) to the GitHub issue
    { pattern = '\\((\\w+\\s)?#([0-9]+)\\)', replace = "([#${2}](<REPO>/issues/${2}))" },
]

commit_parsers = [
    { message = "^feat",     group = "<!-- 0 -->🚀 Features" },
    { message = "^fix",      group = "<!-- 1 -->🐛 Bug Fixes" },
    { message = "^refactor", group = "<!-- 2 -->🚜 Refactor" },
    { message = "^doc",      group = "<!-- 3 -->📚 Documentation" },
    { message = "^perf",     group = "<!-- 4 -->⚡ Performance" },
    { message = "^style",    group = "<!-- 5 -->🎨 Styling" },
    { message = "^test",     group = "<!-- 6 -->🧪 Testing" },

    # Internal-only commits — kept out of the changelog
    { message = "^chore\\(release\\): prepare for", skip = true },
    { message = "^chore\\(deps.*\\)",               skip = true },
    { message = "^chore\\(pr\\)",                   skip = true },
    { message = "^chore\\(pull\\)",                 skip = true },
    { message = "(?i)^bump changelog",              skip = true },
    { message = "^ignore",                          skip = true },

    { message = "^chore|^ci", group = "<!-- 7 -->⚙️ Miscellaneous Tasks" },
    { body    = ".*security", group = "<!-- 8 -->🛡️ Security" },
    { message = "^revert",    group = "<!-- 9 -->◀️ Revert" },
    { message = ".*",         group = "<!-- 10 -->💼 Other" },
]

filter_commits = false
tag_pattern = "v[0-9].*"
skip_tags = "beta|alpha"
ignore_tags = "rc"
topo_order = false
sort_commits = "newest"
